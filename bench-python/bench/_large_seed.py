"""Generated bulk modules for the large seed. See large_fixtures.py."""
BULK = {'shop/catalog.py': '"""Product catalogue, catalog module."""\nfrom dataclasses import dataclass, field\n\n\n@dataclass\nclass Product:\n    """A product record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"catalog:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Variant:\n    """A variant record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"catalog:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Category:\n    """A category record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"catalog:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\ndef catalog_helper_00(records, threshold=0):\n    """Filter and summarise product catalogue records, variant 0."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 0,\n    }\n\n\ndef catalog_helper_01(records, threshold=0):\n    """Filter and summarise product catalogue records, variant 1."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 1,\n    }\n\n\ndef catalog_helper_02(records, threshold=0):\n    """Filter and summarise product catalogue records, variant 2."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 2,\n    }\n\n\ndef catalog_helper_03(records, threshold=0):\n    """Filter and summarise product catalogue records, variant 3."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 3,\n    }\n\n\ndef catalog_helper_04(records, threshold=0):\n    """Filter and summarise product catalogue records, variant 4."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 4,\n    }\n\n\ndef catalog_helper_05(records, threshold=0):\n    """Filter and summarise product catalogue records, variant 5."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 5,\n    }\n\n\ndef catalog_helper_06(records, threshold=0):\n    """Filter and summarise product catalogue records, variant 6."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 6,\n    }\n\n\ndef catalog_helper_07(records, threshold=0):\n    """Filter and summarise product catalogue records, variant 7."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 7,\n    }\n\n\ndef catalog_helper_08(records, threshold=0):\n    """Filter and summarise product catalogue records, variant 8."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 8,\n    }\n\n\ndef catalog_helper_09(records, threshold=0):\n    """Filter and summarise product catalogue records, variant 9."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 9,\n    }\n\n\ndef catalog_helper_10(records, threshold=0):\n    """Filter and summarise product catalogue records, variant 10."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 10,\n    }\n\n\ndef catalog_helper_11(records, threshold=0):\n    """Filter and summarise product catalogue records, variant 11."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 11,\n    }\n\n\ndef catalog_helper_12(records, threshold=0):\n    """Filter and summarise product catalogue records, variant 12."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 12,\n    }\n\n\ndef catalog_helper_13(records, threshold=0):\n    """Filter and summarise product catalogue records, variant 13."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 13,\n    }\n\n', 'shop/pricing.py': '"""Price rules, pricing module."""\nfrom dataclasses import dataclass, field\n\n\n@dataclass\nclass PriceRule:\n    """A pricerule record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"pricing:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Tier:\n    """A tier record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"pricing:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Coupon:\n    """A coupon record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"pricing:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\ndef pricing_helper_00(records, threshold=0):\n    """Filter and summarise price rules records, variant 0."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 0,\n    }\n\n\ndef pricing_helper_01(records, threshold=0):\n    """Filter and summarise price rules records, variant 1."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 1,\n    }\n\n\ndef pricing_helper_02(records, threshold=0):\n    """Filter and summarise price rules records, variant 2."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 2,\n    }\n\n\ndef pricing_helper_03(records, threshold=0):\n    """Filter and summarise price rules records, variant 3."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 3,\n    }\n\n\ndef pricing_helper_04(records, threshold=0):\n    """Filter and summarise price rules records, variant 4."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 4,\n    }\n\n\ndef pricing_helper_05(records, threshold=0):\n    """Filter and summarise price rules records, variant 5."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 5,\n    }\n\n\ndef pricing_helper_06(records, threshold=0):\n    """Filter and summarise price rules records, variant 6."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 6,\n    }\n\n\ndef pricing_helper_07(records, threshold=0):\n    """Filter and summarise price rules records, variant 7."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 7,\n    }\n\n\ndef pricing_helper_08(records, threshold=0):\n    """Filter and summarise price rules records, variant 8."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 8,\n    }\n\n\ndef pricing_helper_09(records, threshold=0):\n    """Filter and summarise price rules records, variant 9."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 9,\n    }\n\n\ndef pricing_helper_10(records, threshold=0):\n    """Filter and summarise price rules records, variant 10."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 10,\n    }\n\n\ndef pricing_helper_11(records, threshold=0):\n    """Filter and summarise price rules records, variant 11."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 11,\n    }\n\n\ndef pricing_helper_12(records, threshold=0):\n    """Filter and summarise price rules records, variant 12."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 12,\n    }\n\n\ndef pricing_helper_13(records, threshold=0):\n    """Filter and summarise price rules records, variant 13."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 13,\n    }\n\n', 'shop/cart.py': '"""Shopping cart, cart module."""\nfrom dataclasses import dataclass, field\n\n\n@dataclass\nclass Cart:\n    """A cart record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"cart:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass CartLine:\n    """A cartline record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"cart:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Reservation:\n    """A reservation record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"cart:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\ndef cart_helper_00(records, threshold=0):\n    """Filter and summarise shopping cart records, variant 0."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 0,\n    }\n\n\ndef cart_helper_01(records, threshold=0):\n    """Filter and summarise shopping cart records, variant 1."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 1,\n    }\n\n\ndef cart_helper_02(records, threshold=0):\n    """Filter and summarise shopping cart records, variant 2."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 2,\n    }\n\n\ndef cart_helper_03(records, threshold=0):\n    """Filter and summarise shopping cart records, variant 3."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 3,\n    }\n\n\ndef cart_helper_04(records, threshold=0):\n    """Filter and summarise shopping cart records, variant 4."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 4,\n    }\n\n\ndef cart_helper_05(records, threshold=0):\n    """Filter and summarise shopping cart records, variant 5."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 5,\n    }\n\n\ndef cart_helper_06(records, threshold=0):\n    """Filter and summarise shopping cart records, variant 6."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 6,\n    }\n\n\ndef cart_helper_07(records, threshold=0):\n    """Filter and summarise shopping cart records, variant 7."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 7,\n    }\n\n\ndef cart_helper_08(records, threshold=0):\n    """Filter and summarise shopping cart records, variant 8."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 8,\n    }\n\n\ndef cart_helper_09(records, threshold=0):\n    """Filter and summarise shopping cart records, variant 9."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 9,\n    }\n\n\ndef cart_helper_10(records, threshold=0):\n    """Filter and summarise shopping cart records, variant 10."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 10,\n    }\n\n\ndef cart_helper_11(records, threshold=0):\n    """Filter and summarise shopping cart records, variant 11."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 11,\n    }\n\n\ndef cart_helper_12(records, threshold=0):\n    """Filter and summarise shopping cart records, variant 12."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 12,\n    }\n\n\ndef cart_helper_13(records, threshold=0):\n    """Filter and summarise shopping cart records, variant 13."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 13,\n    }\n\n', 'shop/checkout.py': '"""Checkout flow, checkout module."""\nfrom dataclasses import dataclass, field\n\n\n@dataclass\nclass Checkout:\n    """A checkout record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"checkout:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Step:\n    """A step record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"checkout:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Validation:\n    """A validation record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"checkout:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\ndef checkout_helper_00(records, threshold=0):\n    """Filter and summarise checkout flow records, variant 0."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 0,\n    }\n\n\ndef checkout_helper_01(records, threshold=0):\n    """Filter and summarise checkout flow records, variant 1."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 1,\n    }\n\n\ndef checkout_helper_02(records, threshold=0):\n    """Filter and summarise checkout flow records, variant 2."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 2,\n    }\n\n\ndef checkout_helper_03(records, threshold=0):\n    """Filter and summarise checkout flow records, variant 3."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 3,\n    }\n\n\ndef checkout_helper_04(records, threshold=0):\n    """Filter and summarise checkout flow records, variant 4."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 4,\n    }\n\n\ndef checkout_helper_05(records, threshold=0):\n    """Filter and summarise checkout flow records, variant 5."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 5,\n    }\n\n\ndef checkout_helper_06(records, threshold=0):\n    """Filter and summarise checkout flow records, variant 6."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 6,\n    }\n\n\ndef checkout_helper_07(records, threshold=0):\n    """Filter and summarise checkout flow records, variant 7."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 7,\n    }\n\n\ndef checkout_helper_08(records, threshold=0):\n    """Filter and summarise checkout flow records, variant 8."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 8,\n    }\n\n\ndef checkout_helper_09(records, threshold=0):\n    """Filter and summarise checkout flow records, variant 9."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 9,\n    }\n\n\ndef checkout_helper_10(records, threshold=0):\n    """Filter and summarise checkout flow records, variant 10."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 10,\n    }\n\n\ndef checkout_helper_11(records, threshold=0):\n    """Filter and summarise checkout flow records, variant 11."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 11,\n    }\n\n\ndef checkout_helper_12(records, threshold=0):\n    """Filter and summarise checkout flow records, variant 12."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 12,\n    }\n\n\ndef checkout_helper_13(records, threshold=0):\n    """Filter and summarise checkout flow records, variant 13."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 13,\n    }\n\n', 'shop/shipping.py': '"""Carrier rates, shipping module."""\nfrom dataclasses import dataclass, field\n\n\n@dataclass\nclass Carrier:\n    """A carrier record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"shipping:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Rate:\n    """A rate record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"shipping:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Zone:\n    """A zone record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"shipping:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\ndef shipping_helper_00(records, threshold=0):\n    """Filter and summarise carrier rates records, variant 0."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 0,\n    }\n\n\ndef shipping_helper_01(records, threshold=0):\n    """Filter and summarise carrier rates records, variant 1."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 1,\n    }\n\n\ndef shipping_helper_02(records, threshold=0):\n    """Filter and summarise carrier rates records, variant 2."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 2,\n    }\n\n\ndef shipping_helper_03(records, threshold=0):\n    """Filter and summarise carrier rates records, variant 3."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 3,\n    }\n\n\ndef shipping_helper_04(records, threshold=0):\n    """Filter and summarise carrier rates records, variant 4."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 4,\n    }\n\n\ndef shipping_helper_05(records, threshold=0):\n    """Filter and summarise carrier rates records, variant 5."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 5,\n    }\n\n\ndef shipping_helper_06(records, threshold=0):\n    """Filter and summarise carrier rates records, variant 6."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 6,\n    }\n\n\ndef shipping_helper_07(records, threshold=0):\n    """Filter and summarise carrier rates records, variant 7."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 7,\n    }\n\n\ndef shipping_helper_08(records, threshold=0):\n    """Filter and summarise carrier rates records, variant 8."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 8,\n    }\n\n\ndef shipping_helper_09(records, threshold=0):\n    """Filter and summarise carrier rates records, variant 9."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 9,\n    }\n\n\ndef shipping_helper_10(records, threshold=0):\n    """Filter and summarise carrier rates records, variant 10."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 10,\n    }\n\n\ndef shipping_helper_11(records, threshold=0):\n    """Filter and summarise carrier rates records, variant 11."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 11,\n    }\n\n\ndef shipping_helper_12(records, threshold=0):\n    """Filter and summarise carrier rates records, variant 12."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 12,\n    }\n\n\ndef shipping_helper_13(records, threshold=0):\n    """Filter and summarise carrier rates records, variant 13."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 13,\n    }\n\n', 'shop/payments.py': '"""Payment methods, payments module."""\nfrom dataclasses import dataclass, field\n\n\n@dataclass\nclass Method:\n    """A method record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"payments:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Token:\n    """A token record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"payments:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Attempt:\n    """A attempt record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"payments:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\ndef payments_helper_00(records, threshold=0):\n    """Filter and summarise payment methods records, variant 0."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 0,\n    }\n\n\ndef payments_helper_01(records, threshold=0):\n    """Filter and summarise payment methods records, variant 1."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 1,\n    }\n\n\ndef payments_helper_02(records, threshold=0):\n    """Filter and summarise payment methods records, variant 2."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 2,\n    }\n\n\ndef payments_helper_03(records, threshold=0):\n    """Filter and summarise payment methods records, variant 3."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 3,\n    }\n\n\ndef payments_helper_04(records, threshold=0):\n    """Filter and summarise payment methods records, variant 4."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 4,\n    }\n\n\ndef payments_helper_05(records, threshold=0):\n    """Filter and summarise payment methods records, variant 5."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 5,\n    }\n\n\ndef payments_helper_06(records, threshold=0):\n    """Filter and summarise payment methods records, variant 6."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 6,\n    }\n\n\ndef payments_helper_07(records, threshold=0):\n    """Filter and summarise payment methods records, variant 7."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 7,\n    }\n\n\ndef payments_helper_08(records, threshold=0):\n    """Filter and summarise payment methods records, variant 8."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 8,\n    }\n\n\ndef payments_helper_09(records, threshold=0):\n    """Filter and summarise payment methods records, variant 9."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 9,\n    }\n\n\ndef payments_helper_10(records, threshold=0):\n    """Filter and summarise payment methods records, variant 10."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 10,\n    }\n\n\ndef payments_helper_11(records, threshold=0):\n    """Filter and summarise payment methods records, variant 11."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 11,\n    }\n\n\ndef payments_helper_12(records, threshold=0):\n    """Filter and summarise payment methods records, variant 12."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 12,\n    }\n\n\ndef payments_helper_13(records, threshold=0):\n    """Filter and summarise payment methods records, variant 13."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 13,\n    }\n\n', 'shop/accounts.py': '"""Customer accounts, accounts module."""\nfrom dataclasses import dataclass, field\n\n\n@dataclass\nclass Account:\n    """A account record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"accounts:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Address:\n    """A address record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"accounts:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Consent:\n    """A consent record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"accounts:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\ndef accounts_helper_00(records, threshold=0):\n    """Filter and summarise customer accounts records, variant 0."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 0,\n    }\n\n\ndef accounts_helper_01(records, threshold=0):\n    """Filter and summarise customer accounts records, variant 1."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 1,\n    }\n\n\ndef accounts_helper_02(records, threshold=0):\n    """Filter and summarise customer accounts records, variant 2."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 2,\n    }\n\n\ndef accounts_helper_03(records, threshold=0):\n    """Filter and summarise customer accounts records, variant 3."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 3,\n    }\n\n\ndef accounts_helper_04(records, threshold=0):\n    """Filter and summarise customer accounts records, variant 4."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 4,\n    }\n\n\ndef accounts_helper_05(records, threshold=0):\n    """Filter and summarise customer accounts records, variant 5."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 5,\n    }\n\n\ndef accounts_helper_06(records, threshold=0):\n    """Filter and summarise customer accounts records, variant 6."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 6,\n    }\n\n\ndef accounts_helper_07(records, threshold=0):\n    """Filter and summarise customer accounts records, variant 7."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 7,\n    }\n\n\ndef accounts_helper_08(records, threshold=0):\n    """Filter and summarise customer accounts records, variant 8."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 8,\n    }\n\n\ndef accounts_helper_09(records, threshold=0):\n    """Filter and summarise customer accounts records, variant 9."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 9,\n    }\n\n\ndef accounts_helper_10(records, threshold=0):\n    """Filter and summarise customer accounts records, variant 10."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 10,\n    }\n\n\ndef accounts_helper_11(records, threshold=0):\n    """Filter and summarise customer accounts records, variant 11."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 11,\n    }\n\n\ndef accounts_helper_12(records, threshold=0):\n    """Filter and summarise customer accounts records, variant 12."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 12,\n    }\n\n\ndef accounts_helper_13(records, threshold=0):\n    """Filter and summarise customer accounts records, variant 13."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 13,\n    }\n\n', 'shop/inventory.py': '"""Stock ledger, inventory module."""\nfrom dataclasses import dataclass, field\n\n\n@dataclass\nclass Stock:\n    """A stock record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"inventory:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Movement:\n    """A movement record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"inventory:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Warehouse:\n    """A warehouse record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"inventory:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\ndef inventory_helper_00(records, threshold=0):\n    """Filter and summarise stock ledger records, variant 0."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 0,\n    }\n\n\ndef inventory_helper_01(records, threshold=0):\n    """Filter and summarise stock ledger records, variant 1."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 1,\n    }\n\n\ndef inventory_helper_02(records, threshold=0):\n    """Filter and summarise stock ledger records, variant 2."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 2,\n    }\n\n\ndef inventory_helper_03(records, threshold=0):\n    """Filter and summarise stock ledger records, variant 3."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 3,\n    }\n\n\ndef inventory_helper_04(records, threshold=0):\n    """Filter and summarise stock ledger records, variant 4."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 4,\n    }\n\n\ndef inventory_helper_05(records, threshold=0):\n    """Filter and summarise stock ledger records, variant 5."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 5,\n    }\n\n\ndef inventory_helper_06(records, threshold=0):\n    """Filter and summarise stock ledger records, variant 6."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 6,\n    }\n\n\ndef inventory_helper_07(records, threshold=0):\n    """Filter and summarise stock ledger records, variant 7."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 7,\n    }\n\n\ndef inventory_helper_08(records, threshold=0):\n    """Filter and summarise stock ledger records, variant 8."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 8,\n    }\n\n\ndef inventory_helper_09(records, threshold=0):\n    """Filter and summarise stock ledger records, variant 9."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 9,\n    }\n\n\ndef inventory_helper_10(records, threshold=0):\n    """Filter and summarise stock ledger records, variant 10."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 10,\n    }\n\n\ndef inventory_helper_11(records, threshold=0):\n    """Filter and summarise stock ledger records, variant 11."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 11,\n    }\n\n\ndef inventory_helper_12(records, threshold=0):\n    """Filter and summarise stock ledger records, variant 12."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 12,\n    }\n\n\ndef inventory_helper_13(records, threshold=0):\n    """Filter and summarise stock ledger records, variant 13."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 13,\n    }\n\n', 'shop/promotions.py': '"""Campaign engine, promotions module."""\nfrom dataclasses import dataclass, field\n\n\n@dataclass\nclass Campaign:\n    """A campaign record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"promotions:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Condition:\n    """A condition record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"promotions:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Reward:\n    """A reward record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"promotions:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\ndef promotions_helper_00(records, threshold=0):\n    """Filter and summarise campaign engine records, variant 0."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 0,\n    }\n\n\ndef promotions_helper_01(records, threshold=0):\n    """Filter and summarise campaign engine records, variant 1."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 1,\n    }\n\n\ndef promotions_helper_02(records, threshold=0):\n    """Filter and summarise campaign engine records, variant 2."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 2,\n    }\n\n\ndef promotions_helper_03(records, threshold=0):\n    """Filter and summarise campaign engine records, variant 3."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 3,\n    }\n\n\ndef promotions_helper_04(records, threshold=0):\n    """Filter and summarise campaign engine records, variant 4."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 4,\n    }\n\n\ndef promotions_helper_05(records, threshold=0):\n    """Filter and summarise campaign engine records, variant 5."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 5,\n    }\n\n\ndef promotions_helper_06(records, threshold=0):\n    """Filter and summarise campaign engine records, variant 6."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 6,\n    }\n\n\ndef promotions_helper_07(records, threshold=0):\n    """Filter and summarise campaign engine records, variant 7."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 7,\n    }\n\n\ndef promotions_helper_08(records, threshold=0):\n    """Filter and summarise campaign engine records, variant 8."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 8,\n    }\n\n\ndef promotions_helper_09(records, threshold=0):\n    """Filter and summarise campaign engine records, variant 9."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 9,\n    }\n\n\ndef promotions_helper_10(records, threshold=0):\n    """Filter and summarise campaign engine records, variant 10."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 10,\n    }\n\n\ndef promotions_helper_11(records, threshold=0):\n    """Filter and summarise campaign engine records, variant 11."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 11,\n    }\n\n\ndef promotions_helper_12(records, threshold=0):\n    """Filter and summarise campaign engine records, variant 12."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 12,\n    }\n\n\ndef promotions_helper_13(records, threshold=0):\n    """Filter and summarise campaign engine records, variant 13."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 13,\n    }\n\n', 'shop/returns.py': '"""Returns and refunds, returns module."""\nfrom dataclasses import dataclass, field\n\n\n@dataclass\nclass Return:\n    """A return record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"returns:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Reason:\n    """A reason record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"returns:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Refund:\n    """A refund record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"returns:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\ndef returns_helper_00(records, threshold=0):\n    """Filter and summarise returns and refunds records, variant 0."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 0,\n    }\n\n\ndef returns_helper_01(records, threshold=0):\n    """Filter and summarise returns and refunds records, variant 1."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 1,\n    }\n\n\ndef returns_helper_02(records, threshold=0):\n    """Filter and summarise returns and refunds records, variant 2."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 2,\n    }\n\n\ndef returns_helper_03(records, threshold=0):\n    """Filter and summarise returns and refunds records, variant 3."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 3,\n    }\n\n\ndef returns_helper_04(records, threshold=0):\n    """Filter and summarise returns and refunds records, variant 4."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 4,\n    }\n\n\ndef returns_helper_05(records, threshold=0):\n    """Filter and summarise returns and refunds records, variant 5."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 5,\n    }\n\n\ndef returns_helper_06(records, threshold=0):\n    """Filter and summarise returns and refunds records, variant 6."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 6,\n    }\n\n\ndef returns_helper_07(records, threshold=0):\n    """Filter and summarise returns and refunds records, variant 7."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 7,\n    }\n\n\ndef returns_helper_08(records, threshold=0):\n    """Filter and summarise returns and refunds records, variant 8."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 8,\n    }\n\n\ndef returns_helper_09(records, threshold=0):\n    """Filter and summarise returns and refunds records, variant 9."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 9,\n    }\n\n\ndef returns_helper_10(records, threshold=0):\n    """Filter and summarise returns and refunds records, variant 10."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 10,\n    }\n\n\ndef returns_helper_11(records, threshold=0):\n    """Filter and summarise returns and refunds records, variant 11."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 11,\n    }\n\n\ndef returns_helper_12(records, threshold=0):\n    """Filter and summarise returns and refunds records, variant 12."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 12,\n    }\n\n\ndef returns_helper_13(records, threshold=0):\n    """Filter and summarise returns and refunds records, variant 13."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 13,\n    }\n\n', 'shop/tax.py': '"""Tax jurisdictions, tax module."""\nfrom dataclasses import dataclass, field\n\n\n@dataclass\nclass Jurisdiction:\n    """A jurisdiction record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"tax:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Band:\n    """A band record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"tax:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Exemption:\n    """A exemption record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"tax:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\ndef tax_helper_00(records, threshold=0):\n    """Filter and summarise tax jurisdictions records, variant 0."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 0,\n    }\n\n\ndef tax_helper_01(records, threshold=0):\n    """Filter and summarise tax jurisdictions records, variant 1."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 1,\n    }\n\n\ndef tax_helper_02(records, threshold=0):\n    """Filter and summarise tax jurisdictions records, variant 2."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 2,\n    }\n\n\ndef tax_helper_03(records, threshold=0):\n    """Filter and summarise tax jurisdictions records, variant 3."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 3,\n    }\n\n\ndef tax_helper_04(records, threshold=0):\n    """Filter and summarise tax jurisdictions records, variant 4."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 4,\n    }\n\n\ndef tax_helper_05(records, threshold=0):\n    """Filter and summarise tax jurisdictions records, variant 5."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 5,\n    }\n\n\ndef tax_helper_06(records, threshold=0):\n    """Filter and summarise tax jurisdictions records, variant 6."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 6,\n    }\n\n\ndef tax_helper_07(records, threshold=0):\n    """Filter and summarise tax jurisdictions records, variant 7."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 7,\n    }\n\n\ndef tax_helper_08(records, threshold=0):\n    """Filter and summarise tax jurisdictions records, variant 8."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 8,\n    }\n\n\ndef tax_helper_09(records, threshold=0):\n    """Filter and summarise tax jurisdictions records, variant 9."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 9,\n    }\n\n\ndef tax_helper_10(records, threshold=0):\n    """Filter and summarise tax jurisdictions records, variant 10."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 10,\n    }\n\n\ndef tax_helper_11(records, threshold=0):\n    """Filter and summarise tax jurisdictions records, variant 11."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 11,\n    }\n\n\ndef tax_helper_12(records, threshold=0):\n    """Filter and summarise tax jurisdictions records, variant 12."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 12,\n    }\n\n\ndef tax_helper_13(records, threshold=0):\n    """Filter and summarise tax jurisdictions records, variant 13."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 13,\n    }\n\n', 'shop/reporting.py': '"""Aggregates, reporting module."""\nfrom dataclasses import dataclass, field\n\n\n@dataclass\nclass Metric:\n    """A metric record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"reporting:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Rollup:\n    """A rollup record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"reporting:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\n@dataclass\nclass Window:\n    """A window record."""\n    ident: str\n    label: str = ""\n    active: bool = True\n    meta: dict = field(default_factory=dict)\n\n    def key(self):\n        return f"reporting:{self.ident}"\n\n    def describe(self):\n        state = "active" if self.active else "archived"\n        return f"{self.label or self.ident} ({state})"\n\n\ndef reporting_helper_00(records, threshold=0):\n    """Filter and summarise aggregates records, variant 0."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 0,\n    }\n\n\ndef reporting_helper_01(records, threshold=0):\n    """Filter and summarise aggregates records, variant 1."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 1,\n    }\n\n\ndef reporting_helper_02(records, threshold=0):\n    """Filter and summarise aggregates records, variant 2."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 2,\n    }\n\n\ndef reporting_helper_03(records, threshold=0):\n    """Filter and summarise aggregates records, variant 3."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 3,\n    }\n\n\ndef reporting_helper_04(records, threshold=0):\n    """Filter and summarise aggregates records, variant 4."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 4,\n    }\n\n\ndef reporting_helper_05(records, threshold=0):\n    """Filter and summarise aggregates records, variant 5."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 5,\n    }\n\n\ndef reporting_helper_06(records, threshold=0):\n    """Filter and summarise aggregates records, variant 6."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 6,\n    }\n\n\ndef reporting_helper_07(records, threshold=0):\n    """Filter and summarise aggregates records, variant 7."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 7,\n    }\n\n\ndef reporting_helper_08(records, threshold=0):\n    """Filter and summarise aggregates records, variant 8."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 8,\n    }\n\n\ndef reporting_helper_09(records, threshold=0):\n    """Filter and summarise aggregates records, variant 9."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 9,\n    }\n\n\ndef reporting_helper_10(records, threshold=0):\n    """Filter and summarise aggregates records, variant 10."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 10,\n    }\n\n\ndef reporting_helper_11(records, threshold=0):\n    """Filter and summarise aggregates records, variant 11."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 11,\n    }\n\n\ndef reporting_helper_12(records, threshold=0):\n    """Filter and summarise aggregates records, variant 12."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 12,\n    }\n\n\ndef reporting_helper_13(records, threshold=0):\n    """Filter and summarise aggregates records, variant 13."""\n    kept = [r for r in records if r.active]\n    if threshold:\n        kept = [r for r in kept if len(r.label) >= threshold]\n    return {\n        "count": len(kept),\n        "keys": [r.key() for r in kept],\n        "variant": 13,\n    }\n\n', 'shop/__init__.py': '"""Large e-commerce backend."""\n__version__ = "5.1.0"\n'}
