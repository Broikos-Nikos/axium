# Build output
/target

# Editor temporaries
*.swp
*.swo
*~
.DS_Store
.idea/
.vscode/

# API keys and secrets — NEVER commit
config.json

# Personal runtime data
memory.md
chat_history.db
chat_history.db-shm
chat_history.db-wal
conversation.log

# Agent caches and per-project knowledge
.axium/

# Deployment scripts (contain server-specific paths/credentials) / Compiled binary (if placed in project root)
axiom
deploy.sh
Axium.desktop
Cargo.lock

# Catch-all for log files
*.log
